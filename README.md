# ramah

This repository is a **static website**: the app is built into plain HTML, CSS, and JavaScript and served as fixed files (no server-rendered pages). It is implemented with React and **Vite**, and produces a static bundle via `npm run build` (output in `build/`).
